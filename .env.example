# Postgres connection string (hosted, e.g. Neon, in production; local Postgres
# in dev). For a Homebrew local Postgres: postgresql://localhost:5432/healthmaxxing
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Shared secret the /api/ingest route requires as `Authorization: Bearer <token>`.
# Generate one with: openssl rand -base64 32
INGEST_SECRET=changeme

# Owner secret for the MCP server (/api/mcp). Two roles, same value:
#   1. A raw `Authorization: Bearer <token>` for programmatic clients / the CLI.
#   2. The login password on the OAuth consent screen that the Claude.ai
#      connector opens (the connector requires an OAuth handshake, not a raw
#      bearer). See lib/oauth.ts and app/api/oauth/*.
# Generate one with: openssl rand -base64 32
MCP_SECRET=changeme
